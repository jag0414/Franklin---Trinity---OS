# Production Environment Configuration
# IMPORTANT: This file should be customized for production deployment
# DO NOT commit actual production secrets to version control

# Server Configuration
PORT=3000
NODE_ENV=production

# Database Configuration
# Replace with your production MongoDB connection string
MONGODB_URI=mongodb://mongodb:27017/franklin-trinity-os
DB_NAME=franklin-trinity-os

# JWT Configuration
# CRITICAL: Generate a strong, unique secret for production
# Use: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=REPLACE_WITH_STRONG_RANDOM_SECRET_IN_PRODUCTION
JWT_EXPIRES_IN=7d

# CORS Configuration
# Set to your frontend domain(s) in production
CORS_ORIGIN=https://yourdomain.com

# Logging
LOG_LEVEL=info
